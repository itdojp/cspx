exit_code:
  eq: 1
status:
  eq: "fail"
checks:
  - name:
      eq: "refine"
    model:
      eq: "T"
    status:
      eq: "fail"
    counterexample:
      present: true
      trace_len:
        eq: 1
      tags:
        contains: ["refinement", "model:T"]
