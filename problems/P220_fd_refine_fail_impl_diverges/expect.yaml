exit_code:
  eq: 1
status:
  eq: "fail"
checks:
  - name:
      eq: "refine"
    model:
      eq: "FD"
    status:
      eq: "fail"
    counterexample:
      present: true
      trace_len:
        eq: 1
      tags:
        contains: ["refinement", "model:FD", "divergence_mismatch", "divergence"]
