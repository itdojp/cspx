status:
  eq: "fail"
checks:
  - name:
      eq: "check"
    status:
      eq: "fail"
    counterexample:
      present: true
      trace_len:
        eq: 1
      tags:
        contains: ["deadlock"]
      source_spans:
        any:
          - path:
              contains: "model.cspm"
            start_line:
              eq: 5
    stats:
      states:
        eq: 2
      transitions:
        eq: 1
