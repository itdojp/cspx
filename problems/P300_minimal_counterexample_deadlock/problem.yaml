id: P300
title: minimal counterexample (deadlock)
suite: fast
tags: [deadlock, counterexample, assertion, fast]
run:
  cmd: ["cspx", "check", "--assert", "deadlock free", "model.cspm", "--format", "json"]
