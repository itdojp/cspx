id: P902
title: alternating bit protocol (tiny, generated bench)
suite: bench
tags: [bench, generated, protocol, abp, tiny]
run:
  cmd: ["cspx", "check", "--assert", "deadlock free", "model.cspm", "--format", "json"]
  timeout_ms: 10000
