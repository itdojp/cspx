exit_code:
  eq: 1
status:
  eq: "fail"
checks:
  - name:
      eq: "check"
    status:
      eq: "fail"
    counterexample:
      present: true
      trace_len:
        eq: 1
      tags:
        contains: ["nondeterminism"]
      source_spans:
        any:
          - path:
              contains: "model.cspm"
            start_line:
              eq: 4
    stats:
      states:
        eq: 5
      transitions:
        eq: 5
