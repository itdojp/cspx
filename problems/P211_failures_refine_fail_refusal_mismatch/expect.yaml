exit_code:
  eq: 1
status:
  eq: "fail"
checks:
  - name:
      eq: "refine"
    model:
      eq: "F"
    status:
      eq: "fail"
    counterexample:
      present: true
      trace_len:
        eq: 0
      tags:
        contains: ["refinement", "model:F", "refusal_mismatch", "refuse:b"]
