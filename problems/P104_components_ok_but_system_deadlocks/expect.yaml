status:
  eq: "fail"
checks:
  - name:
      eq: "check"
    status:
      eq: "fail"
    counterexample:
      present: true
      trace_len:
        eq: 0
      tags:
        contains: ["deadlock"]
      source_spans:
        any:
          - path:
              contains: "model.cspm"
            start_line:
              eq: 6
    stats:
      states:
        eq: 1
      transitions:
        eq: 0
